version: '3'
services:

  postgres:
    image: postgres
    container_name: postgres_sql
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5401:5432"
    volumes:
      #- pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    command: [ "postgres", "-c", "log_statement=all" ]

  frontend:
    image: funtown/frontend
    depends_on:
      - "postgres"
    ports:
      - 3000:3000

  application:
    image: funtown/application
    depends_on:
      - "postgres"

  user_service:
    image: funtown/user_service
    depends_on:
      - "postgres"

